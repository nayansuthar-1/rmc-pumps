import mongoose from 'mongoose'


const admin = new mongoose.Schema({

    username:{
        type:String,
        required:true,
        
    },
      username:{
        type:String,
        required:true,
        
    },
      username:{
        type:String,
        required:true,
        
    },
})